# install-post-fill — Post-Fill Pass Orchestrator

## Purpose
Installs the post-fill pass on the kernel. Triggers confirm mirror, corrections observer, and mirror observer after sequential fill completes.

## Calls
`installPostFillConfirm`, `installPostFillCorrections`, `installPostFillMirror`
