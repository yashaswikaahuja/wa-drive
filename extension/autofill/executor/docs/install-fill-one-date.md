# install-fill-one-date — Date Handler Registration

## Purpose
Registers the `date` handler in `k.fillOneHandlers`. Delegates all fill logic to `CcFillOneDate`.

## Delegates to
- `CcFillOneDate` (CAP-19) — flatpickr, jQuery datepicker, mat-datepicker, native date inputs
