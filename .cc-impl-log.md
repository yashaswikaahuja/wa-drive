## 2026-08-09 — Runtime Fill Pipeline Fix Session

### Problems Fixed
1. **413 Payload Too Large** — `/api/fill-plan` route didn't exist in extension-service; requests fell through to hub (100KB limit). Created route + nginx location blocks on both LBs.
2. **Empty Plan (knowledge_records empty)** — The new fill-planner reads `knowledge_records` table, not the legacy `form_mappings` store. Table was never seeded. Created `load-seed.js` idempotent loader, loaded 124 records (field_mappings + synonyms + derivation_rules).
3. **Wrong Value (father→name collision)** — `matchLabelScore` used first-match; changed to longest-match-wins. Also normalized underscores to spaces in patterns.
4. **Null accessible_name (all nodes)** — New `computeAccessibleName` only had label[for] + wrapping label + basic preceding sibling. SSC Angular uses container-label and parent-hierarchy patterns. Ported full 8-strategy `getLabel` from legacy `shared/dom-utils.js`.
5. **Context_id mismatch** — Popup hardcoded `'ctx_top_level'` fallback; perception generates `ctx.top.1`. Fixed to use `step.target.context_id` from plan.
6. **Inject order** — dom-gateway now injected FIRST before perception modules.
7. **Context counter reset** — `resetContextCounter()` called before each perceive for consistent IDs.
8. **AI key** — GROQ/OpenRouter keys added to container env; `ai-key-manager` falls back to them.
9. **GHCR auth on cybercontrol-ext** — Re-authenticated via gcloud SSH.

### Architecture Compliance
- No live Element refs in PageSnapshot/ActionPlan (BindingRegistry stays browser-private)
- Extension does not plan when server returns empty (shows diagnostic instead)
- All planning/AI/knowledge stays server-side

### Commits (master)
- `879d832` — IIFE perception isolation
- `44b3f2d` — /fill-plan + /fill-observation routes + nginx
- `fe0f6f6` — Hub 5MB body limit
- `40c25d7` — Seed knowledge_records + longest-match + profile aliases
- `9d1292b` — Binding fixes (liveElements, resolveTarget, registry-first execution)
- `49a87e9` — context_id fix, inject order, counter reset, empty-plan UX
- `3cd839d` — Underscore→space normalization in matchLabelScore
- `c3d4707` — Full 8-strategy getLabel port to computeAccessibleName

### Remaining
- Real SSC fill test after extension reload (need user to reload unpacked)
- `ng-select` div dropdowns still get `(none)` labels (no preceding sibling for custom components)
- AI provider "openrouter" not implemented in ai-key-manager `callAI()` — needs HTTP fetch to OpenRouter
- Session logging of fill results (user requested)