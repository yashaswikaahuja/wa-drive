# URL of the dashboard hub (Service 1)
HUB_URL=https://your-dashboard-hub.onrender.com

# Must match WORKER_SECRET on the hub
WORKER_SECRET=change-me-to-a-strong-secret

# Optional: Google Drive OAuth access token (set via dashboard UI instead)
DRIVE_ACCESS_TOKEN=
