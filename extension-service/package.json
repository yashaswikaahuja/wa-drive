{
  "name": "cybercontrol-extension-service",
  "version": "1.0.0",
  "description": "Standalone API for the Chrome extension — profiles, mappings, adapters",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "start": "node dist/index.js",
    "dev": "node --watch index.js",
    "typecheck": "node -e \"console.log('[extension-service] typecheck skipped (JS)')\"",
    "test": "node -e \"console.log('[extension-service] no package tests yet')\""
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^17.0.1",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
