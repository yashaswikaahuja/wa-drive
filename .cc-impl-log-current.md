---
title: ''
---

## 2026-08-09 ??? Runtime Fill Pipeline Fix Session
### Problems Fixed
1. **413 Payload Too Large** ??? `/api/fill-plan` route + nginx location blocks
2. **Empty Plan (knowledge_records empty)** ??? `load-seed.js` loaded 124 records
3. **Wrong Value (father???name collision)** ??? Longest-match-wins + underscore normalization
4. **Null accessible_name** ??? Ported full 8-strategy getLabel from legacy dom-utils
5. **ng-select labels null (#113 Bug 1)** ??? Extended isFormField to cover ng-select, mat-select, role=combobox, formcontrolname
6. **OpenRouter callAI fails (#113 Bug 2)** ??? Added openrouter/groq to dispatchToProvider switch (OpenAI-compatible) + HTTP-Referer headers
7. **Context_id mismatch** ??? Uses [step.target](http://step.target).context_id from plan, not hardcoded
8. **Inject order** ??? dom-gateway.js injected FIRST
9. **Context counter reset** ??? resetContextCounter() before each perceive
10. **GHCR auth** ??? Re-authenticated on cybercontrol-ext via gcloud
### Key Commits
- `171b0c3` ??? Fix #113 (ng-select + OpenRouter)
- `c3d4707` ??? Full 8-strategy getLabel port
- `3cd839d` ??? Underscore normalization in matchLabelScore
- `9d1292b` ??? Binding fixes
- `40c25d7` ??? Seed knowledge_records
- `44b3f2d` ??? fill-plan/fill-observation routes
### Remaining
- Session logging of fill results (what filled, what failed)
- Real SSC test after extension reload
- Complex widget execution (ng-dropdown option selection via capability plugins)
- Remove debug logging from fill route
### Architecture Compliance
- Extension = Eyes + Hands only (perceive, execute, report)
- Server = Brain + Memory + Knowledge (plan, map, learn)
- No live Element refs in PageSnapshot/ActionPlan
- BindingRegistry browser-private, memory-only

## 2026-08-09 — Discussion 11 Product-Path Conformance Audit
### Verified P0 corrections (local working tree; not yet committed or deployed)
- Replaced `popup.js` inline execution and accessible-name/selector fallback with `runtime/action-plan-executor.js`.
- Enforced ActionPlan v3 expiry, authorization/risk, exact `document_id` + `snapshot_id` + revision binding, binding generation, adapter, and affordance checks.
- Added delayed mechanical postcondition verification and canonical ExecutionObservation v3 output with redacted value states only.
- `plan-builder.js` now resolves `select_option` to a real public option `(context_id,node_id)`; no `option:*:pending` placeholder or fuzzy extension-side option search. Selects without a perceived option are omitted safely.
- Added persistent execution evidence: `/api/fill-observation` accepts the v3 body, updates fill-session progress, writes per-step filled/failed/skipped reasons plus plan/session/snapshot identity to PostgreSQL `sessions.records`, and routes linked outcomes to the learning engine.
- Fixed cold-start: partial plans now map unresolved nodes; current-session high-confidence AI candidates can be used while knowledge remains draft; semantic mapper reads PageSnapshot v2 `observed.accessible_name`.
- Fixed eligibility conflation: privacy-sensitive PII (Aadhaar/PAN) remains PROFILE_DATA; only secret auth/challenge data is excluded.
- Removed temporary fill-route payload debug logs.
- Added extension ZIP build provenance (`build-info.json`) and extension-service image/health commit provenance; restored Groq/OpenRouter env parity in repository compose.

### Validation
- Standard CI: 744 tests passed across 15 suites.
- Product-path regression: 17/17.
- Browser perception: 112/112.
- Security: 102/102.
- Schema ratification: 26/26.
- Fill HTTP routes: 12/12.
- DOM boundary check: PASS.
- Disposable extension package: includes manifest, build provenance, v3 executor, gateway, perception, popup.

### Remaining nonconformities / verification gates
- Production deployment and real SSC + ServicePlus side-panel E2E are not run yet; production mutation requires explicit approval.
- PostgreSQL persistence smoke could not run locally because Docker is unavailable; route logic is validated, but live DB insertion must be verified after deployment.
- WSS `/ws` remains non-canonical in production: client is not instantiated on Fill, nginx has no extension-service `/ws` route, and `createHandlers()` is not wired to the orchestrator. Current requested product path remains HTTP.
- Legacy Agent/DISPATCH_JOB/teach paths still contain grandfathered selector/local-intelligence behavior and are not the side-panel Fill path.
- Dynamic custom selects whose options are absent from PageSnapshot are now safely skipped rather than fuzzily operated; behavioral discovery/teach remains required for full coverage.
