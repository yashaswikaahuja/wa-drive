{
  "name": "wa-drive",
  "private": true,
  "version": "0.0.0",
  "description": "CyberControl monorepo — extension capability packages",
  "scripts": {
    "test": "pnpm -r test",
    "build": "node build-all.mjs"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
