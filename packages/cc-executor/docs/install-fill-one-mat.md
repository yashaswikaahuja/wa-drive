# install-fill-one-mat — Angular Material Handler Registration

## Purpose
Registers the `mat` handler in `k.fillOneHandlers`. Delegates all fill logic to `CcFillOneMat`.

## Delegates to
- `CcFillOneMat` (CAP-17) — mat-select, mat-checkbox, mat-radio
