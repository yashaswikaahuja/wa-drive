# install-fill-one-radio-planned — Radio-Click/Radio-Group Handler Registration

## Purpose
Registers the `radio-planned` handler in `k.fillOneHandlers`. Delegates to `CcFillOneRadio`.

## Delegates to
- `CcFillOneRadio` (CAP-18) — radio-click, radio-group with gender synonyms
