# install-post-fill-corrections — Correction Observer Wiring

## Purpose
Installs the correction observer on the kernel context. Delegates all logic to `CcPostFillCorrections`.

## Delegates to
- `CcPostFillCorrections` (CAP-21) — snapshot, submit listener, POST corrections
