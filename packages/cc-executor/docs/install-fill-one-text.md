# install-fill-one-text — Text/Keystroke Handler Registration

## Purpose
Registers the `text` handler in `k.fillOneHandlers`. Delegates all fill logic to `CcFillOneText`.

## Delegates to
- `CcFillOneText` (CAP-15) — keystrokeFillSync, legacy value-set, Hindi sibling
