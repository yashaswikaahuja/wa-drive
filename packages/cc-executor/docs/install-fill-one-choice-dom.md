# install-fill-one-choice-dom — Radio/Checkbox/File Handler Registration

## Purpose
Registers the `choice-dom` handler in `k.fillOneHandlers`. Delegates all fill logic to `CcFillOneRadio`.

## Delegates to
- `CcFillOneRadio` (CAP-18) — radio DOM, checkbox, file (base64)
