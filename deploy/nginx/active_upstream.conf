# Switchable upstream — rewritten by bluegreen-deploy.sh / bluegreen-rollback.sh, then `nginx -s reload`.
# Active color's port: blue=3001, green=3002.
upstream backend_active { server 127.0.0.1:3001; }
